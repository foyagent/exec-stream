{
  "id": "exec-stream",
  "name": "Exec Stream",
  "description": "实时展示 OpenClaw 命令执行过程",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "port": {
        "type": "number",
        "default": 9200,
        "description": "WebSocket 服务端口（仅 local 模式生效）"
      },
      "mode": {
        "type": "string",
        "enum": ["local", "remote"],
        "default": "local",
        "description": "local=本地服务器, remote=连接远程服务器"
      },
      "remoteServer": {
        "type": "string",
        "description": "远程服务器地址 (remote 模式必填)，如 https://nas.local:9200"
      },
      "remoteToken": {
        "type": "string",
        "description": "远程服务器认证 Token (remote 模式用)"
      },
      "jwtSecret": {
        "type": "string",
        "description": "JWT 签名密钥（建议使用 SecretRef）"
      },
      "tokenExpiry": {
        "type": "number",
        "default": 172800,
        "description": "Token 有效期（秒），默认 48h"
      }
    }
  }
}
